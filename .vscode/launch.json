{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "node-terminal",
      "request": "launch",
      "name": "Run React App (npm run dev)",
      "command": "npm run dev",
      "cwd": "${workspaceFolder}"
    }
  ]
}
